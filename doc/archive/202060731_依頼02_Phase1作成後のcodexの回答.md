Phase 1のコード更新は完了し、Phase 2には進んでいません。以前のご希望どおり本番ビルドは実行していないため、完了条件のうち「本番ビルド成功」だけ未確認です。

## Phase 1報告

1. 既存構成
   4章の本文・SVG・状態・処理が1,341行の単一ファイルへ集中していました。画面はヒーロー、進捗、章ナビ、章見出し、章内容、章末操作で構成されていました。

2. 分割方針
   既存表示を変えず、親画面、教材レジストリ、章別コンポーネント、共通UIに分割しました。src/components/basics/SurveyBasics.tsx:11は193行になっています。

3. 作成ファイル
   教材定義：src/components/basics/basicsCourse.ts:7、src/components/basics/types.ts:1
   章別：lessons/の4ファイル
   共通UI：ui/のCheckIcon、LessonNavigation、LessonHeader、LessonFooter

4. 変更ファイル
   SurveyBasics.tsx、scripts/basics-smoke.mjs、doc/HANDOFF.md:863

5. 変更していない機能
   4章の文章・初期値・操作・計算結果、CSS、Appの教材切替、閉合トラバースの計算・型・データ・操作・学習記録は変更していません。

6. 章ID・レジストリ
   point-and-position、distance-and-direction、height-difference、error-and-equipment。章番号・名称・説明・用語・コンポーネントをレジストリで管理します。

7. 共通化UI
   章ナビ、章見出し・用語タグ、章末操作、完了チェックアイコンを分離しました。

8. 角度処理
   既存のnormalizeAzimuth、decimalDegreesToDms、degreesToRadiansを再利用しています。表示上の空白を含む度分秒形式は維持しました。

9. TypeScript型検査
   成功、エラー0件。

10. 単体テスト
   8ファイル、41テストすべて成功。

11. 本番ビルド
   ご希望に従い未実施です。次を実行してください。

   npm run build

12. Playwright
   基礎教材スモーク、閉合トラバースPhase 4回帰スモークとも成功。コンソールエラー・ページ例外は0件です。

13. 画面幅
   1366px・390pxとも横方向のはみ出しなし。一時スクリーンショットも目視確認済みです。

14. 依存ファイル
   package.json、package-lock.jsonは開始時とSHA-256が一致し、差分・新規依存はありません。

15. 残る確認
   本番ビルド結果のみ未確認です。

16. Phase 2
   教材仕様のデータ化、9章登録枠、準備中状態などが予定されていますが、今回は着手していません。
