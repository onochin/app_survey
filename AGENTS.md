# 測量理解ラボ Codex共通作業ルール

## 1. 正本と優先順位

作業開始前に、必ず次を確認する。

1. 最新の `doc/HANDOFF.md`
2. 対象依頼のMarkdown
3. 初期仕様の確認が必要な場合のみ依頼01

内容が異なる場合は、最新の `doc/HANDOFF.md`を優先する。

## 2. 作業開始時

最初に次を実行し、既存差分を確認する。

```bash
git status --short
```

既存差分を破棄、上書き、無関係に整形しない。

禁止事項：

* `git reset`
* `git checkout -- .`
* `git restore .`
* 無関係な名称変更
* 無関係なリファクタリング
* 関係のないファイルの整形

## 3. 実装方針

* 指定されたPhaseだけを実施する
* 次のPhaseを先行実装しない
* 最小差分とする
* 現在の章別コンポーネントと教材レジストリを維持する
* 安定した文字列IDを変更しない
* 既存のテスト済み処理を再利用する
* 教材データ、純粋関数、UIを可能な範囲で分離する
* 将来利用する可能性だけを理由に大規模な共通化を行わない
* 不明な仕様を推測で確定しない

## 4. 必ず維持する仕様

* 実装済み章の文章、図、操作、計算結果
* 教材往復時の操作状態
* `App.tsx`の両教材常時マウント
* `.basics-*`のCSS名前空間
* 閉合トラバースの計算、型、データ、操作
* 閉合トラバースの確認問題と学習記録
* 閉合トラバース学習記録の保存キー

```text
survey-learning-lab:traverse-learning-records:v1
```

## 5. 変更禁止

* 新しいパッケージの追加
* 外部APIの追加
* `package.json`の変更
* `package-lock.json`の変更
* npm本体の更新
* npmのグローバルインストール
* `sudo npm install`
* Playwrightブラウザ保存方式の変更
* 既存スクリーンショットの上書き
* 指定前の確認問題・学習記録実装
* 指定前のCSS分割や大規模最適化

Viteの500 kB警告だけを理由に、大規模なコード分割や最適化を行わない。

## 6. UIと状態管理

* 既存教材のデザインと共通UIを再利用する
* 第9章などの章専用CSSも`.basics-`から始める
* 接頭辞のないグローバルCSSを追加しない
* 通常説明文は13px、補助説明は12pxを基準とする
* 色だけで状態を区別しない
* 1366pxと390pxでページ全体を横にはみ出させない
* 操作状態は、別途指定がない限りReactの画面状態だけで管理する
* `NaN`、`Infinity`、`undefined`を画面へ表示しない

## 7. 必須検証

最低限、次を実行する。

```bash
npm run typecheck -- --pretty false
npm test -- --reporter=verbose
npm run build
node --check scripts/basics-smoke.mjs
git diff --check
git diff -- package.json package-lock.json
```

最新HANDOFFに従って、次も実行する。

* 基礎教材Playwrightスモーク
* 閉合トラバースPhase 4回帰スモーク
* 1366px表示確認
* 390px表示確認
* コンソールエラー確認
* ページ例外確認
* 外部API通信確認
* 教材往復時の状態保持確認

既存スクリーンショットを上書きしない。

必要な確認画像は`/tmp`または新しいファイル名へ保存する。

## 8. HANDOFF更新

作業完了時に`doc/HANDOFF.md`へ、次を追記する。

* Phaseの完了状況
* 実装内容
* 教材メタデータ
* 追加したデータと純粋関数
* 作成・変更ファイル
* 維持した既存機能
* 型検査結果
* 単体テスト結果
* ビルド結果
* Playwright結果
* 1366px・390px確認結果
* packageファイルの差分有無
* 残る注意点
* 次回開始地点

指定されたPhaseの実装、検証、HANDOFF更新が完了したら停止する。

## 9. 完了報告

日本語で、次の順番で簡潔に報告する。

1. 結論
2. 変更内容
3. 検証結果
4. 注意点

テスト数、ビルドのmodules数、成果物サイズ、警告の有無も報告する。
